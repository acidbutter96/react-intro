import React from "react";
import {Button} from "reactstrap";

export default (props)=>{
    return(
        <Button color="danger" style={{fontSize:40}}>
            Danger motherfucker
        </Button>
    );
};